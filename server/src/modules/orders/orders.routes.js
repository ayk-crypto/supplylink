import createPlaceholderModule from "../createPlaceholderModule.js";

const ordersRoutes = createPlaceholderModule({
  key: "orders",
  label: "Orders",
  scope: "vendor",
  description: "Ordering module foundation spanning order capture, items, fulfillment, and delivery linkage."
});

export default ordersRoutes;
