import createPlaceholderModule from "../createPlaceholderModule.js";

const productsRoutes = createPlaceholderModule({
  key: "products",
  label: "Products",
  scope: "vendor",
  description: "Vendor catalog and category module for future inventory and pricing workflows."
});

export default productsRoutes;
