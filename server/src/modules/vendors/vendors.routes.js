import createPlaceholderModule from "../createPlaceholderModule.js";

const vendorsRoutes = createPlaceholderModule({
  key: "vendors",
  label: "Vendors",
  scope: "platform",
  description: "Vendor onboarding, subscription assignment, and tenant lifecycle management."
});

export default vendorsRoutes;
