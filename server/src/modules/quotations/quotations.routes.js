import createPlaceholderModule from "../createPlaceholderModule.js";

const quotationsRoutes = createPlaceholderModule({
  key: "quotations",
  label: "Quotations",
  scope: "vendor",
  description: "Quotation workflow base for proposal generation and conversion into orders."
});

export default quotationsRoutes;
