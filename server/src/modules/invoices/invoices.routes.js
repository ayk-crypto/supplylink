import createPlaceholderModule from "../createPlaceholderModule.js";

const invoicesRoutes = createPlaceholderModule({
  key: "invoices",
  label: "Invoices",
  scope: "vendor",
  description: "Invoice lifecycle module prepared for billing, balances, and payment reconciliation."
});

export default invoicesRoutes;
