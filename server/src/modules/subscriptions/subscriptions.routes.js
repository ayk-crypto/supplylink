import createPlaceholderModule from "../createPlaceholderModule.js";

const subscriptionsRoutes = createPlaceholderModule({
  key: "subscriptions",
  label: "Subscriptions",
  scope: "platform",
  description: "Platform subscription and billing-plan management foundation for vendor tenants."
});

export default subscriptionsRoutes;
