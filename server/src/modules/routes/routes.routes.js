import createPlaceholderModule from "../createPlaceholderModule.js";

const routesRoutes = createPlaceholderModule({
  key: "routes",
  label: "Routes",
  scope: "vendor",
  description: "Delivery route planning base with driver assignment and sequenced route stops."
});

export default routesRoutes;
